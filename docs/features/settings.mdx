---
title: "App Settings"
description: "Configure startup behavior, notifications, defaults, and system preferences"
icon: "gear"
---

App Settings provides a centralized configuration panel for Claude Board. Access it from the **gear icon** on the Dashboard, or from the **App Settings** entry in the project dropdown menu.

## Accessing Settings

<Steps>
  <Step title="From Dashboard" icon="grid-2">
    Click the gear icon in the top-right corner of the Dashboard header, next to the language selector.
  </Step>
  <Step title="From Project View" icon="chevron-down">
    Open the project dropdown menu and click **App Settings** in the 3x3 settings grid.
  </Step>
</Steps>

## General Settings

### Launch at Startup

Automatically open Claude Board when your computer starts. Uses the operating system's native auto-launch mechanism:

| Platform | Mechanism |
|----------|-----------|
| **Windows** | Registry startup entry |
| **macOS** | LaunchAgent |

<Info>This setting is only available in the Tauri desktop app, not in web mode.</Info>

### Minimize to Tray

When enabled, closing the window hides Claude Board to the **system tray** instead of quitting. The app continues running in the background and can be restored by:

- **Clicking** the tray icon (left-click)
- **Right-click** → **Claude Board** from the tray context menu
- To fully quit: **Right-click** → **Quit**

<Warning>This is a desktop-only feature. The system tray icon is always visible when the app is running.</Warning>

### Confirm Before Delete

Show a confirmation dialog before deleting tasks or projects. Enabled by default.

### Auto-Open Terminal

Automatically open the terminal panel when a task starts running. Disable this if you prefer to manually open the terminal.

### Default Model

Set the default Claude model for new tasks:

| Model | Description |
|-------|-------------|
| **Haiku** | Fast and lightweight |
| **Sonnet** | Balanced (default) |
| **Opus** | Most capable |

### Default Thinking Effort

Set the default thinking effort level for new tasks:

| Level | Description |
|-------|-------------|
| **Low** | Quick, surface-level analysis |
| **Medium** | Balanced depth (default) |
| **High** | Deep reasoning and exploration |

### Language

Switch the application display language. Currently supported:

- **English**
- **Turkish** (Turkce)

The language change takes effect immediately across all UI elements, including notification messages.

## Models

The Models tab lists every model available in the task and template model pickers, split into two groups.

### Synced models

This list comes from [models.dev](https://models.dev) and refreshes in the background at most once every 24 hours, so newly released Claude models and price changes arrive without an app update. **Refresh** forces a sync immediately, and the header shows when the list was last updated.

Alongside each released model, the list carries two kinds of entry that models.dev does not publish, because they are Claude Code CLI syntax rather than API model ids:

- **Family aliases** — `haiku`, `sonnet`, `opus`, `fable`. Each tracks the newest release in its family and inherits that model's pricing.
- **1M-context variants** — `claude-opus-4-8[1m]` and the like, generated for every model whose context window is 1M. They inherit the base model's pricing; the premium rate that applies above 200k tokens is not published upstream and so is not modelled here.

When a sync fails — no network, or an unreadable response — the previous list stays in place and the failure is recorded in the log rather than shown as an error.

### Custom models

**Add Model** creates an entry for a model id the synced list does not carry. Editing a synced model also produces a custom entry: your version overrides the synced one and is never overwritten by a later sync.

Two controls apply to custom entries:

- **Reset** discards your changes and hands the model back to the synced list.
- **Delete** removes a model from the pickers for good. A deleted model stays deleted — the next sync will not bring it back. Re-add it with **Add Model** if you change your mind.

## Settings Storage

Settings are stored in the local SQLite database (`app_settings` table) as key-value pairs. Changes are saved immediately when toggled — there is no separate "Save" button.

## API

Settings are available through the API:

```
GET  /api/settings          # Get all settings
PUT  /api/settings          # Update settings (partial update)
```

For Tauri desktop:
```javascript
api.getAppSettings()
api.updateAppSettings({ key: value })
```
